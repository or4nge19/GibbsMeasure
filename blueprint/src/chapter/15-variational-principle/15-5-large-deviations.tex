\section{Large deviations and equivalence of ensembles}

\begin{definition}[The periodic empirical field, Georgii (15.41)--(15.44)]
    \label{def:ld-15-41}
    \uses{def:erg-invariant, thm:erg-14A8}
    \lean{MeasureTheory.GibbsMeasure.periodicEmpiricalField, MeasureTheory.GibbsMeasure.abs_integral_periodicEmpiricalField_sub_le, MeasureTheory.GibbsMeasure.tendsto_integral_periodicEmpiricalField_sub_zero, MeasureTheory.GibbsMeasure.ae_tendsto_integral_periodicEmpiricalField, MeasureTheory.GibbsMeasure.tendsto_measure_setOf_abs_integral_periodicEmpiricalField_sub_lt}
    \leanok

    The periodic empirical field $°R^\omega_\Delta$ is shift invariant, differs from $R^\omega_\Delta$
    by at most $2c\abs T/\abs\Delta$ uniformly in $\omega$ (15.43)(1), and for an ergodic
    $\mu \in \Prob_\Theta$ satisfies $°R^\omega_{\Lambda_n}(f) \to \mu(f)$ almost surely along
    increasing cubes and any torus reductions (15.43)(2), hence
    $\mu(°R_{\Lambda_n} \in U) \to 1$ for every basic neighbourhood $U$ of $\mu$ (15.44).
\end{definition}

\begin{theorem}[Shannon--McMillan, the input of Georgii (15.45)--(15.47)]
    \label{thm:ld-shannon-mcmillan}
    \lean{MeasureTheory.GibbsMeasure.specificEntropy_uniformOn_eq_entropyRate_sub_log_card, MeasureTheory.GibbsMeasure.entropyIn_uniformOn_eq_neg_integral_log_measureReal, MeasureTheory.GibbsMeasure.tendsto_integral_abs_neg_inv_card_mul_log_measureReal_sub_entropyRate, MeasureTheory.GibbsMeasure.tendsto_integral_abs_inv_card_mul_log_density_add_specificEntropy, MeasureTheory.blockProb, MeasureTheory.condProbGiven, MeasureTheory.condExp_indicator_comap_ae_eq_condProbGiven, MeasureTheory.condInformation, MeasureTheory.condInformation_ae_eq_neg_log_condProbGiven, MeasureTheory.entropyRate, MeasureTheory.pastWindow, MeasureTheory.neg_log_blockProb_eq_sum_condInformation, MeasureTheory.integral_exp_condInformation_sub_le_one, MeasureTheory.integral_abs_le_of_integral_exp_le_one, MeasureTheory.exists_forall_integral_abs_condInformation_sub_le, MeasureTheory.tendsto_integral_abs_neg_inv_card_mul_log_blockProb_sub_entropyRate, MeasureTheory.tendsto_inv_card_mul_integral_neg_log_blockProb, MeasureTheory.tendsto_integral_abs_neg_inv_card_mul_log_blockProb_sub_entropyRate_cube, MeasureTheory.condExp_ae_eq_integral_of_forall_measure_eq_zero_or_one}
    \leanok

    Let $G$ be a countable linearly ordered abelian group acting measurably on $\Omega$
    preserving an ergodic probability measure $\mu$, $X : \Omega \to E$ measurable with $E$
    finite, and $p_\Lambda(\omega) = \mu(X_i = X_i(\omega)\ \forall i \in \Lambda)$ the block
    probabilities. The entropy rate $h$ is the infimum over finite past windows $W \subset
    \{i < 0\}$ of $\int g_W\,d\mu$, $g_W = -\log \mu(X_0 = X_0(\omega) \mid X_W)$. Along every
    F\o lner net $(\Lambda_k)$, $\int \bigl| -|\Lambda_k|^{-1}\log p_{\Lambda_k} - h \bigr|\,d\mu
    \to 0$; on $\mathbb Z^d$ with the lexicographic order this holds along any sequence of cubes
    with side lengths tending to infinity. This is the $L^1$ form Georgii uses in (15.47).
    For $\mu \in \mathcal P_\Theta$ on $E^{\mathbb Z^d}$ with $E$ finite, the entropy rate of
    the origin given the lexicographic past and the specific entropy of \S15.2 relative to the
    uniform distribution are related by $\mathfrak k(\mu) = h - \log|E|$, without ergodicity;
    hence, for ergodic $\mu$ and $f_\Lambda = |E|^{|\Lambda|}\mu(\sigma_\Lambda = \cdot)$,
    Georgii's displayed statement $\mu\bigl(\bigl||\Lambda_n|^{-1}\log f_{\Lambda_n} +
    \mathfrak k(\mu)\bigr|\bigr) \to 0$ along cubes.
\end{theorem}
\begin{proof}
    \leanok
    The exact chain rule $-\log p_\Lambda(\omega) = \sum_{i \in \Lambda} g_{W_i(\Lambda)}(i +
    \omega)$ over the past windows $W_i(\Lambda) = (\Lambda \cap \{j < i\}) - i$; for $W' \subseteq
    W$, $\int e^{g_W - g_{W'}} \leq 1$, and $\int e^u \leq 1$ on a probability space gives
    $\int|u| \leq 2\delta + (1 + 2/\delta)(-\int u)$, so the $g_W$ are uniformly $L^1$-close to
    $g_{W_0}$ for all $W \supseteq W_0$ once $\int g_{W_0}$ is within $\varepsilon$ of $h$;
    the F\o lner property makes the sites whose window misses $W_0$ negligible, and the ergodic
    theorem in $L^1$ replaces $|\Lambda_k|^{-1}\sum_i g_{W_0}(i + \omega)$ by $\int g_{W_0}$.
    The identification with $\mathfrak k(\mu)$ compares the two limits along the cubes
    $[-n,0]^d$, using Shannon's formula $\mathcal H_\Lambda(\mu \mid \bar\lambda^\Lambda) =
    -\int \log \mu(\sigma_\Lambda = \sigma_\Lambda(\omega))\,d\mu - |\Lambda|\log|E|$
    for the uniform $\bar\lambda$; no upward martingale theorem is needed.
\end{proof}

\begin{theorem}[Choquet barycentres, Phelps Prop.\ 1.2 and Lemma 9.7, the input of Step 3 of (15.46)]
    \label{thm:ld-barycenter}
    \lean{MeasureTheory.IsBarycenter, MeasureTheory.isBarycenter_dirac, MeasureTheory.IsBarycenter.smul_add_smul, MeasureTheory.IsBarycenter.unique, MeasureTheory.IsBarycenter.integral_continuousAffineMap, MeasureTheory.exists_isBarycenter_of_isCompact, MeasureTheory.existsUnique_isBarycenter, MeasureTheory.IsBarycenter.mem_closedConvexHull, MeasureTheory.exists_isBarycenter_of_mem_closedConvexHull, MeasureTheory.mem_closedConvexHull_iff_exists_isBarycenter, MeasureTheory.isBarycenter_integral}
    \leanok

    In a Hausdorff real topological vector space, $x$ is the barycentre of a Borel probability
    measure $m$ if $\ell(x) = \int \ell\,dm$ for every continuous linear functional $\ell$. A
    probability measure carried by a compact convex $K$ has a barycentre in $K$, unique when the
    space is locally convex; for closed $C \subseteq K$, the points of the closed convex hull of
    $C$ are exactly the barycentres of probability measures carried by $C$. No metrizability is
    needed: Mathlib's compactness of the probability measures on a compact Hausdorff space
    replaces Georgii's passage to the metrizable $\mathcal C$-topology.
\end{theorem}
\begin{proof}
    \leanok
    Existence by Jensen's inequality for closed convex sets applied to finitely many functionals
    and the finite intersection property; uniqueness and the converse inclusion by the geometric
    Hahn--Banach theorem; the representation by Prokhorov compactness of the probability measures
    on $C$, the set of barycentres of such measures being closed, convex and containing $C$.
    Georgii's Lemma 9.7 further evaluates an affine lower semicontinuous $[0,\infty]$-valued
    functional at a barycentre as its integral; that identity is not yet proved.
\end{proof}

\begin{proposition}[Georgii (15.49), (15.59), and the rate-function half of (15.48)]
    \label{prop:ld-15-49}
    \uses{def:ld-15-41, thm:vp-15-39, prop:sent-15-14c, cor:gv-16-12}
    \lean{Potential.BTheta.ldRate, Potential.BTheta.ldRate_nonneg, Potential.BTheta.ldRate_eq_zero_iff, Potential.BTheta.ldRate_smul_add_smul_le, Potential.BTheta.isCompact_setOf_ldRate_le, Potential.BTheta.lowerSemicontinuous_ldRate, Potential.BTheta.ldRate_eq_iSup, Potential.BTheta.iSup_sub_ldRate_eq, Potential.BTheta.exists_specificRelativeEntropy_eq_pressure_sub, Potential.BTheta.iInf_ldRate_interior_eq_iInf_closure, eq_iSup_sub_of_isClosed_convex_epigraph}
    \leanok

    The two displayed inequalities of (15.48) are not proved, since they rest on (15.45). Its
    remaining content: the rate function $J_\Psi(\cdot\mid\Phi)$ is nonnegative, vanishes exactly on the Gibbs
    measures, is convex with compact level sets and lower semicontinuous, and is the Legendre
    transform $J_\Psi(x\mid\Phi) = \sup_t [t\cdot x - P(\Phi - t\cdot\Psi) + P(\Phi)]$, the
    conjugate being attained at a shift-invariant Gibbs measure of $\Phi - t\cdot\Psi$. For convex
    $B$ meeting the interior, the infimum over $\bar B$ and over $B^\circ$ agree (15.59).
\end{proposition}
\begin{proof}
    \leanok
    Fenchel--Moreau for a rate function: the epigraph is closed and convex, so a separating
    hyperplane in $\mathbb R^k \times \mathbb R$ gives the supremum representation, with the
    variational principle supplying attainment.
\end{proof}
